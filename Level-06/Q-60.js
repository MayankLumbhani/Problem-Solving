function array(arr){
    let sum = 0;
    for(let i = 0; i < arr.length; i++){
        sum += arr[i];
    }
    avg = sum/arr.length;
    console.log(avg);
}

array([1, 2, 3, 4]);
array([5, 15]);
array([7]);
